%nav.navbar.navbar-default
  .container
    .navbar-header
      - if user_signed_in?
        =link_to "You are awesome, #{current_user.username.capitalize}!", current_user, class: "navbar-brand"
      - else
        .navbar-brand Workout App
    .nav.navbar-nav.navbar-right
      - if user_signed_in?
        %li= link_to "New Workout", new_workout_path
        %li= link_to "My Workouts", root_path
        %li= link_to "My Exercises", exercises_path
        %li= link_to "Log Out", destroy_user_session_path, method: :delete
      - else
        %li= link_to "Log In", new_user_session_path
        %li= link_to "Sign Up", new_user_registration_path